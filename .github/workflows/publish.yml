name: Publish packages

on:
  push:
    tags: ["*"]

permissions:
  contents: write
  id-token: write

jobs:
  github:
    uses: tree-sitter/workflows/.github/workflows/release.yml@main
    with:
      generate: true
